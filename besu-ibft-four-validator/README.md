### Start

```
sudo docker-compose -f docker-compose.grafana.yml up -d
```
```
sudo docker-compose -f docker-compose.fluent-bit.yml up -d
```
```
sudo docker-compose -f docker-compose.node1.yml up -d
```
```
sudo docker-compose -f docker-compose.node2.yml up -d
```
```
sudo docker-compose -f docker-compose.node3.yml up -d
```
```
sudo docker-compose -f docker-compose.node4.yml up -d
```